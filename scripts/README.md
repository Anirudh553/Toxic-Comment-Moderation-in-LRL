# Scripts

Keep one-off utility scripts here, such as:

- dataset download helpers
- preprocessing runners like `python -m src.data.prepare`
- training launch scripts
- transformer launch scripts like `python -m src.training.train_transformer`
- evaluation runners
